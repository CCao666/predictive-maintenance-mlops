[pytest]
testpaths = tests
pythonpath = .
norecursedirs =
    upstream-reference
    .venv
    .git
    __pycache__
addopts = -ra
markers =
    integration: requires the isolated compose.ci.yaml services
